import React, { Component, PropTypes } from 'react';

class Resource extends Component {
  render() {
    return (
      <div>
        Resource
      </div>
    );
  }
}

Resource.propTypes = {
};

export default Resource;
